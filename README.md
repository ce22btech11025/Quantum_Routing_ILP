1. Create new virtual environment
2. Using 'pip install -r requirements.txt' and download the necessary modules
3. Then run python main2.py and change the number of nodes, edges and demands
4. You can change seed in datagen.py for generating different outputs
